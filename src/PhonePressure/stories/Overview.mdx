import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs/blocks'

import FormStories, { Success } from './Form.stories'

<Meta of={FormStories} />

# `<PhonePressureForm>`

Este formulário realiza ligações telefônicas síncronas via [Twilio](https://www.twilio.com/pt-br).

<Canvas of={Success} />

## Props

<ArgTypes/>

`PhoneCallState` é uma string.

`PhoneTarget` possui o seguinte formato:

```ts
interface PhoneTarget {
  name: string
  number: string
}
```

`PhoneCallAction` possui o seguinte formato:

```ts
type PhoneCallAction = (payload: PhonePressureAction, setState: SetState<PhoneCallState>) => Promise<void>
```

`PhonePressureAction` possui o seguinte formato:

```ts
interface PhonePressureAction {
  activist: {
    name: string
    email: string
    phone: string
  }

  input: {
    custom_fields: {
      target: PhoneTarget
    }
  }

  widget_id: number
}
```
